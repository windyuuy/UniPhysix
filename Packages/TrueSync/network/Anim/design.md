
# 确定性动画设计

<!-- // TODO: 确定性动画设计 -->

运行时动态替换方案:
加载时检测动画组件, 如果对象有动画组件和帧同步组件, 那么:
- 挂载 LogicAnimPlayer 组件.


LogicAnimPlayer组件:
- 加载
	- 传入动画组件和动画对象
	- 预播放动画组件和动画对象, 采集帧动画样本并存储.
- 播放逻辑动画
	- 获取预先采集的动画样本, 在每次逻辑帧调度时, 设置动画对象属性, 通过帧同步组件来驱动动画播放, 控制逻辑对象实时属性.
	